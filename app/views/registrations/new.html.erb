<div class="mb-3 text-center">
  <h1>Sign Up</h1>
</div>

<%= form_with model: @user, url: sign_up_path do |form| %>
  <!-- alert error message(s) -->
  <%= render "shared/form_errors", form: form %>

  <div class="shadow p-3 mt-5 mb-3 bg-body rounded">
    <!-- form fields -->
    <div class="mb-3">
      <%= form.label :email %>
      <%= form.text_field :email, class: "form-control", placeholder: "user@email.com" %>
    </div>
    <div class="mb-3">
      <%= form.label :password %>
      <%= form.password_field :password, class: "form-control", placeholder: "password" %>
    </div>
    <div class="mb-3">
      <%= form.label :password_confirmation %>
      <%= form.password_field :password_confirmation, class: "form-control", placeholder: "re-enter password" %>
    </div>
  </div>

  <!-- submit button -->
  <div class="d-flex justify-content-between">
    <%= form.submit "Sign Up", class: "btn btn-primary" %> 
    <div>
      <%= link_to "Login", sign_in_path, class:"mx-1"%> f you already have an account
    </div>
  </div>
<% end %>
